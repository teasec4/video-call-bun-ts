# Документация проекта WebRTC Video Calling

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура](#архитектура)
3. [Текущая реализация](#текущая-реализация)
4. [Планы на улучшения](#планы-на-улучшения)
5. [Рефакторинг](#рефакторинг)

---

## Обзор проекта

WebRTC приложение для видеозвонков с поддержкой чата. Реализовано на React (frontend) и Bun (backend).

### Технологический стек

**Frontend:**
- React 19
- TypeScript
- TailwindCSS 4
- React Router DOM 7

**Backend:**
- Bun runtime
- WebSocket для signaling
- REST API для управления комнатами

**WebRTC:**
- STUN серверы (Google)
- P2P видеосвязь
- Поддержка нескольких камер

---

## Архитектура

### Структура проекта

```
reacttest/
├── backend/                    # Backend сервер (Bun)
│   ├── index.ts               # Точка входа
│   └── src/
│       ├── config/
│       │   └── constants.ts   # Константы (PORT)
│       ├── handlers/
│       │   ├── httpHandler.ts  # HTTP API endpoints
│       │   └── wsHandler.ts    # WebSocket обработка
│       ├── services/
│       │   ├── ClientManager.ts    # Управление клиентами
│       │   ├── MessageStore.ts    # Хранение сообщений чата
│       │   └── RoomManager.ts     # Управление комнатами
│       ├── types/
│       │   └── index.ts        # TypeScript типы
│       └── server.ts           # Основной сервер
│
├── src/                        # Frontend (React)
│   ├── components/
│   │   ├── chatArea.tsx        # Компонент чата
│   │   ├── roomBadge.tsx       # Бейдж комнаты
│   │   ├── roomControl.tsx     # Управление комнатами
│   │   └── videoArea.tsx       # Основной компонент видео
│   ├── hooks/
│   │   ├── useWebSocket.ts     # Хук для WebSocket
│   │   └── useWebRTC.ts        # Хук для WebRTC
│   ├── pages/
│   │   ├── HomePage.tsx        # Главная страница
│   │   └── RoomPage.tsx        # Страница комнаты
│   ├── types/
│   │   └── webrtc.ts           # Типы WebRTC
│   ├── App.tsx                 # Главный компонент
│   └── index.ts                # Точка входа
│
└── package.json
```

### Поток данных

```
┌─────────────┐
│   Client A  │
└──────┬──────┘
       │
       │ WebSocket (signaling)
       │
┌──────▼──────────────────┐
│   Backend Server        │
│  - RoomManager          │
│  - ClientManager        │
│  - MessageStore         │
└──────┬──────────────────┘
       │
       │ WebSocket (signaling)
       │
┌──────▼──────┐
│   Client B  │
└─────────────┘

       │
       │ WebRTC P2P
       │
┌──────▼──────┐
│   Client A  │◄──────►│   Client B  │
└─────────────┘        └─────────────┘
```

---

## Текущая реализация

### Backend

#### Сервисы

**ClientManager** (`backend/src/services/ClientManager.ts`)
- Управление WebSocket соединениями клиентов
- Группировка клиентов по комнатам
- Broadcast сообщений в комнаты

**MessageStore** (`backend/src/services/MessageStore.ts`)
- Хранение истории сообщений чата
- Фильтрация по комнатам
- Очистка при закрытии комнаты

**RoomManager** (`backend/src/services/RoomManager.ts`)
- Создание комнат
- Присоединение к комнатам
- Отслеживание пиров в комнатах

#### Handlers

**httpHandler** (`backend/src/handlers/httpHandler.ts`)
- `POST /api/room` - создание комнаты
- `GET /api/room/:roomId` - информация о комнате
- `POST /api/room/join` - присоединение к комнате
- `GET /api/messages/:roomId` - история сообщений

**wsHandler** (`backend/src/handlers/wsHandler.ts`)
- Обработка WebSocket соединений
- Перенаправление WebRTC signaling сообщений (offer/answer/ICE candidates)
- Уведомление о подключении пиров
- Broadcast чат сообщений

### Frontend

#### Хуки

**useWebSocket** (`src/hooks/useWebSocket.ts`)
- Управление WebSocket соединением
- Автоматическое переподключение
- Обработка входящих сообщений
- Управление состоянием `remotePeerId`

**API:**
```typescript
const { 
  isConnected,      // Состояние соединения
  remotePeerId,     // ID удаленного пира
  send,             // Отправка сообщений
  disconnect,       // Отключение
  reconnect         // Переподключение
} = useWebSocket({
  roomId,
  peerId,
  onMessage,        // Callback для обработки сообщений
  onPeerConnected, // Callback при подключении пира
  onRoomClosed     // Callback при закрытии комнаты
});
```

**useWebRTC** (`src/hooks/useWebRTC.ts`)
- Управление RTCPeerConnection
- Управление медиа потоками (локальный/удаленный)
- Обработка WebRTC signaling (offer/answer/ICE candidates)
- Управление камерами и устройствами
- Переключение видео/аудио

**API:**
```typescript
const {
  webrtcState,      // Состояние WebRTC
  mediaState,       // Состояние медиа
  initializeLocalStream,
  startCall,
  hangup,
  handleOffer,
  handleAnswer,
  handleIceCandidate,
  toggleVideo,
  toggleAudio,
  switchCamera,
  getCameras,
  localStreamRef,
  remoteStreamRef
} = useWebRTC({
  remotePeerId,
  onSendSignaling,  // Callback для отправки signaling
  onRemoteStream    // Callback при получении удаленного потока
});
```

#### Компоненты

**RoomPage** (`src/pages/RoomPage.tsx`)
- Оркестрация WebSocket и WebRTC
- Управление чатом
- Координация обмена сообщениями между хуками

**VideoArea** (`src/components/videoArea.tsx`)
- UI для видео звонка
- Отображение локального и удаленного видео
- Элементы управления (видео/аудио/камера)
- Информация о комнате

**ChatArea** (`src/components/chatArea.tsx`)
- Интерфейс чата
- Отображение сообщений
- Отправка сообщений

**RoomControl** (`src/components/roomControl.tsx`)
- Создание комнаты
- Присоединение к комнате
- Копирование ссылки на комнату

### WebRTC Flow

1. **Подключение к комнате:**
   - Клиент подключается через WebSocket
   - Сервер отправляет `peer-connected` если есть другой пир

2. **Инициация звонка:**
   - Первый пир создает offer
   - Отправляет через WebSocket
   - Второй пир получает offer, создает answer
   - Отправляет answer обратно

3. **ICE Negotiation:**
   - Оба пира обмениваются ICE candidates
   - Устанавливается P2P соединение

4. **Медиа потоки:**
   - Локальный поток отображается в маленьком окне
   - Удаленный поток отображается в основном окне

### Ключевые особенности реализации

1. **Использование refs для callbacks:**
   - `onSendSignalingRef` и `onRemoteStreamRef` для избежания проблем с замыканиями
   - Актуальные значения в callbacks WebRTC

2. **Защита от повторных вызовов:**
   - Проверки на уже идущий звонок в `startCall` и `handleOffer`
   - Предотвращение конфликтов при одновременных вызовах

3. **Обработка состояний:**
   - Отслеживание состояний PeerConnection (connectionState, iceConnectionState, signalingState)
   - Автоматическая очистка при ошибках

4. **Очередь ICE candidates:**
   - Хранение candidates до установки remote description
   - Обработка после установки

---

## Планы на улучшения

### 1. UI/UX улучшения

#### Приоритет: Высокий

- [ ] **Индикаторы состояния соединения**
  - Визуальные индикаторы качества соединения
  - Показ состояния ICE (connecting/connected/failed)
  - Индикатор задержки (latency)

- [ ] **Улучшение обработки ошибок**
  - Понятные сообщения об ошибках для пользователя
  - Toast уведомления вместо alert
  - Retry механизм для failed соединений

- [ ] **Адаптивный дизайн**
  - Поддержка мобильных устройств
  - Оптимизация для планшетов
  - Responsive layout

- [ ] **Улучшение UI элементов**
  - Анимации переходов
  - Loading состояния
  - Skeleton loaders

### 2. Функциональные улучшения

#### Приоритет: Средний

- [ ] **TURN серверы**
  - Добавление TURN серверов для работы за NAT/firewall
  - Автоматический fallback на TURN при неудаче STUN
  - Конфигурация через environment variables

- [ ] **Запись звонка**
  - Запись локального потока
  - Запись удаленного потока
  - Сохранение в файл

- [ ] **Screen sharing**
  - Демонстрация экрана
  - Переключение между камерой и экраном
  - Выбор окна/вкладки для демонстрации

- [ ] **Улучшение чата**
  - Уведомления о новых сообщениях
  - Timestamp сообщений
  - Emoji поддержка
  - Файлы в чате

- [ ] **Управление звуком**
  - Выбор устройства вывода
  - Тест звука перед звонком
  - Индикатор уровня звука

### 3. Производительность

#### Приоритет: Средний

- [ ] **Оптимизация рендеринга**
  - React.memo для компонентов
  - useMemo для вычислений
  - useCallback для функций
  - Виртуализация списка сообщений в чате

- [ ] **Оптимизация медиа**
  - Адаптивное качество видео (bitrate)
  - Автоматическое снижение качества при плохом соединении
  - Оптимизация разрешения видео

- [ ] **Lazy loading**
  - Code splitting
  - Lazy загрузка компонентов
  - Динамические импорты

### 4. Безопасность

#### Приоритет: Высокий

- [ ] **Валидация данных**
  - Валидация входящих WebSocket сообщений
  - Санитизация пользовательского ввода
  - Rate limiting

- [ ] **Авторизация**
  - Проверка прав доступа к комнатам
  - Токены для доступа
  - Ограничение количества пиров в комнате

- [ ] **HTTPS/WSS**
  - Поддержка HTTPS для production
  - WSS для WebSocket
  - SSL сертификаты

- [ ] **Защита от злоупотреблений**
  - Rate limiting для API
  - Защита от спама в чате
  - Ограничение размера сообщений

### 5. Мониторинг и логирование

#### Приоритет: Низкий

- [ ] **Структурированное логирование**
  - Уровни логирования (debug/info/warn/error)
  - Форматирование логов
  - Логирование важных событий

- [ ] **Метрики**
  - Количество активных комнат
  - Количество активных соединений
  - Статистика использования

- [ ] **Health checks**
  - Endpoint для проверки здоровья сервера
  - Мониторинг WebSocket соединений
  - Алерты при проблемах

### 6. Тестирование

#### Приоритет: Средний

- [ ] **Unit тесты**
  - Тесты для сервисов (ClientManager, RoomManager, MessageStore)
  - Тесты для хуков (useWebSocket, useWebRTC)
  - Тесты для утилит

- [ ] **Integration тесты**
  - Тесты WebRTC flow
  - Тесты WebSocket соединений
  - Тесты API endpoints

- [ ] **E2E тесты**
  - Полный сценарий звонка
  - Тесты UI компонентов
  - Тесты на разных браузерах

---

## Рефакторинг

### 1. Разделение компонентов

#### Приоритет: Низкий

**VideoArea** можно разделить на:
- `VideoControls` - кнопки управления (видео/аудио/камера/завершить)
- `RoomInfo` - информация о комнате
- `CameraSelector` - выбор камеры
- `LocalVideo` - локальное видео
- `RemoteVideo` - удаленное видео

**Преимущества:**
- Лучшая читаемость кода
- Переиспользование компонентов
- Легче тестировать

### 2. Создание контекста

#### Приоритет: Низкий

**WebRTCContext** для глобального состояния:
- Общее состояние WebRTC
- Методы доступные во всех компонентах
- Упрощение пропсов

**Преимущества:**
- Меньше проп drilling
- Централизованное управление состоянием
- Упрощение компонентов

### 3. Улучшение типизации

#### Приоритет: Низкий

- Строгие типы для всех сообщений
- Enum для типов сообщений
- Валидация типов в runtime (zod)

**Преимущества:**
- Меньше ошибок
- Лучшая поддержка IDE
- Автодополнение

### 4. Конфигурация

#### Приоритет: Низкий

- Environment variables для конфигурации
- Конфигурационные файлы
- Разные настройки для dev/prod

**Пример:**
```typescript
// config.ts
export const config = {
  wsUrl: process.env.WS_URL || 'ws://localhost:3030',
  apiUrl: process.env.API_URL || 'http://localhost:3030',
  iceServers: process.env.ICE_SERVERS || [...],
  maxReconnectAttempts: 5,
  reconnectDelay: 1000,
};
```

### 5. Обработка ошибок

#### Приоритет: Средний

- Централизованная обработка ошибок
- Error boundaries в React
- Retry механизмы
- Понятные сообщения об ошибках

### 6. Документация кода

#### Приоритет: Низкий

- JSDoc комментарии
- README для каждого модуля
- Примеры использования
- Диаграммы архитектуры

---

## Рекомендации по разработке

### 1. Стиль кода

- Использовать TypeScript строго
- Следовать ESLint правилам
- Форматировать код с Prettier
- Комментировать сложную логику

### 2. Git workflow

- Семантические коммиты
- Feature branches
- Code review перед merge
- Тесты перед коммитом

### 3. Производительность

- Профилирование перед оптимизацией
- Измерение метрик
- Мониторинг производительности
- Оптимизация только при необходимости

### 4. Безопасность

- Регулярные обновления зависимостей
- Проверка уязвимостей
- Безопасная обработка данных
- Валидация всех входных данных

---

## Заключение

Текущая реализация обеспечивает базовую функциональность видеозвонков. Основные улучшения должны быть направлены на:

1. **Стабильность** - улучшение обработки ошибок и edge cases
2. **UX** - улучшение пользовательского опыта
3. **Производительность** - оптимизация рендеринга и медиа
4. **Безопасность** - валидация и защита от злоупотреблений
5. **Масштабируемость** - подготовка к росту нагрузки

Приоритеты могут меняться в зависимости от требований проекта и обратной связи пользователей.

