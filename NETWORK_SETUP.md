# Настройка для работы с разных устройств

## Проблема

По умолчанию backend слушает только на `localhost`, что делает его недоступным с других устройств в сети.

## Решение

### 1. Backend настроен ✅

Backend слушает на `0.0.0.0:3030`, что означает доступность на всех сетевых интерфейсах.

### 2. Frontend dev server настроен ✅

Frontend dev server (Bun) теперь тоже слушает на `0.0.0.0:3000`, что позволяет открыть его с других устройств.

### 3. Frontend автоматически определяет API URL ✅

Frontend автоматически использует:
- `localhost:3030` если открыт через `localhost`
- IP адрес с портом если открыт через IP (например, `192.168.1.100:3030`)

### 3. Как найти IP адрес вашего компьютера

**macOS/Linux:**
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
# или
ip addr show | grep "inet " | grep -v 127.0.0.1
```

**Windows:**
```bash
ipconfig
# Ищите IPv4 Address в разделе вашего Wi-Fi адаптера
```

Пример: `192.168.1.100`

### 4. Как протестировать с разных устройств

**Пошаговая инструкция:**

1. **Запустите приложение:**
   ```bash
   bun run dev
   ```
   
   Вы увидите в консоли:
   ```
   🚀 Server running at http://localhost:3000/
   🌐 Also available at http://192.168.1.100:3000/
   📱 Use this URL on your phone: http://192.168.1.100:3000/
   ```

2. **На компьютере:**
   - Откройте `http://192.168.1.100:3000` (IP из консоли)
   - Или `http://localhost:3000` (будет работать только локально)

3. **На телефоне (в той же Wi-Fi сети):**
   - Откройте `http://192.168.1.100:3000` (тот же IP из консоли)
   - Frontend автоматически определит правильный URL для API и WebSocket

**Важно:** Используйте IP адрес (не localhost) на обоих устройствах для работы между ними!

**Вариант 2: Ручная настройка**

Если автоматическое определение не работает, можно вручную указать IP в `src/config/api.ts`:

```typescript
// Раскомментируйте и замените на ваш IP:
export const API_URL = 'http://192.168.1.100:3030';
export const WS_URL = 'ws://192.168.1.100:3030';
```

### 5. Проверка

1. Убедитесь что оба устройства в одной Wi-Fi сети
2. Убедитесь что firewall не блокирует порт 3030
3. Проверьте что backend запущен и слушает на `0.0.0.0:3030`
4. Откройте на обоих устройствах приложение через IP адрес

### 6. Troubleshooting

**Проблема: Не могу подключиться с телефона**

- Проверьте что используете IP адрес, а не localhost
- Проверьте firewall настройки
- Убедитесь что оба устройства в одной сети
- Проверьте что backend запущен: `curl http://YOUR_IP:3030/health`

**Проблема: WebSocket не подключается**

- Убедитесь что используете `ws://` для WebSocket (не `http://`)
- Проверьте что порт 3030 открыт
- Проверьте консоль браузера на ошибки

### 7. Production

Для production используйте:
- Доменное имя вместо IP
- HTTPS/WSS вместо HTTP/WS
- Настройте CORS правильно в `backend/src/config/constants.ts`

